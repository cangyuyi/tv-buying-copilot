# AI PRD — 电视选购 Copilot Multi-Agent 导购系统

**版本**：V1.0 → V1.1  
**日期**：2026-08-22  
**文档类型**：AI 产品需求文档（AI PRD）

---

## 一、背景与目标

### 1.1 背景

电视选购是典型的高决策成本场景：
- 用户表达不稳定：不会按标准参数描述需求，常说"小一点的""客厅很亮""玩 PS5"
- 知识来源复杂：面板类型、刷新率、亮度、色域、尺寸与距离关系，用户难以自行判断
- 输出需要解释性：推荐结果需要说明为什么适合，而不是简单给列表
- 传统关键词搜索无法理解自然语言需求，人工客服成本高

传统电商详情页和搜索无法解决"我家客厅 2.5 米，预算 4000，追剧用，买什么电视"这类自然语言查询。

### 1.2 为什么用 AI 解决

| 传统方式 | AI Agent 方式 |
|---|---|
| 用户自己筛选参数，学习成本高 | 自然语言输入，系统自动解析需求 |
| 关键词匹配，无法理解"客厅很亮" | RAG + 结构化筛选，理解场景化需求 |
| 推荐无理由，用户不信任 | 每款推荐附带参数对比和引用来源 |
| 售后问题混入导购，体验差 | 意图分类自动路由，高风险强制转人工 |

### 1.3 目标

**业务目标**：
- 降低用户决策成本，从"翻 20 个商品页"到"3 轮对话拿到推荐"
- 导购场景自助解决率 ≥80%，减少人工客服压力

**模型效果指标**：
- 意图分类准确率 ≥95%
- 硬约束满足率 100%（不推荐超预算/不满足刷新率的商品）
- 引用覆盖率 100%（推荐必须附带知识库引用）
- 回复时长 ≤3 秒（演示模式）

---

## 二、用户场景与流程

### 2.1 核心用户场景

| 场景 | 用户画像 | 典型输入 | 核心需求 |
|---|---|---|---|
| 新手选购 | 第一次买电视，不懂参数 | "预算 4000，客厅用，推荐个电视" | 引导澄清需求，给出易懂推荐 |
| 游戏玩家 | 明确游戏需求 | "三千预算，玩 PS5，要 120Hz 以上" | 筛选高刷低延迟型号 |
| 品质追求 | 追求画质，预算充足 | "预算 8000，看电影玩游戏，75 寸" | 推荐高端 Mini LED/OLED |
| 价格敏感 | 租房/老人/小房间 | "租房过渡，小一点，1500 以内" | 推荐高性价比小尺寸 |
| 优惠咨询 | 已有购买意向 | "现在有什么优惠？以旧换新怎么弄？" | 计算叠加优惠和到手价 |
| 履约查询 | 下单前确认 | "配送要几天？挂装收费吗？" | 说明配送时效和安装费用 |
| 售后问题 | 已下单用户 | "我要退货""电视坏了" | 识别高风险，强制转人工 |

### 2.2 完整用户流程

```
用户进入
  │
  ├─ 输入自然语言需求
  │
  ├─ 系统解析需求（预算/尺寸/用途/刷新率）
  │
  ├─ 信息不足？→ 多轮澄清（每次一个问题，带选项）
  │     │
  │     └─ 信息齐全后继续
  │
  ├─ 意图分类 → 路由到对应 Agent
  │     ├─ 商品咨询 → 商品参数 Agent（RAG检索+筛选+打分）
  │     ├─ 价格优惠 → 比价优惠 Agent（促销规则+叠加计算）
  │     ├─ 履约安装 → 履约服务 Agent（配送/安装政策）
  │     ├─ 售后问题 → 售后客服 Agent（强制转人工）
  │     └─ 非相关 → 兜底回复，引导到电视导购
  │
  ├─ 硬约束二次检查（Replanner）
  │     └─ 移除超预算/不满足刷新率/无库存商品
  │
  ├─ 合规审核（5条红线自检+修正循环）
  │
  └─ 返回推荐结果 + 参数对比 + 引用来源 + 执行轨迹
```

---

## 三、AI 优化设计

### 3.1 整体架构：Master-Worker Multi-Agent

```
                    ┌─────────────────┐
                    │  Master Router  │  确定性意图分类（6类）
                    └────────┬────────┘
                             │
          ┌──────────┬───────┼───────┬──────────┐
          ▼          ▼       ▼       ▼          ▼
    ┌─────────┐ ┌────────┐ ┌──────┐ ┌────────┐ ┌────────┐
    │ 商品参数 │ │ 比价优惠│ │履约  │ │ 售后客服│ │ 需求澄清│
    │ Agent   │ │ Agent  │ │Agent │ │ Agent  │ │ Agent  │
    └────┬────┘ └───┬────┘ └──┬───┘ └───┬────┘ └───┬────┘
         │          │         │          │          │
         └──────────┴────┬────┴──────────┴──────────┘
                         ▼
                  ┌─────────────┐
                  │  Replanner  │  硬约束二次检查
                  └──────┬──────┘
                         ▼
                  ┌─────────────┐
                  │ Compliance  │  5条红线 + 修正循环
                  └──────┬──────┘
                         ▼
                     最终回答
```

### 3.2 节点详细设计

#### 节点 1：NeedParser（需求解析）

| 字段 | 说明 |
|---|---|
| 节点名称 | NeedParser / 需求解析器 |
| 输入 | 用户原始消息（string） |
| 输出 | 结构化槽位（budget/size/distance/use_cases/min_refresh） |
| 处理逻辑 | 正则提取 + 中文数字转换 + 否定句处理 + 分辨率排除 |
| 完成条件 | 所有可识别槽位提取完成 |
| 异常处理 | 无法识别的槽位留空，由澄清 Agent 后续追问 |
| 关键能力 | 中文数字"三千"、缩写"3k"、口语"2米5"、否定"不玩游戏只追剧"、模糊"小一点" |

#### 节点 2：Master Router（意图分类总控）

| 字段 | 说明 |
|---|---|
| 节点名称 | Master Router / 意图分类总控 |
| 输入 | 用户消息 + 已提取槽位 |
| 输出 | route（6类之一）+ target_agents + reason |
| 处理逻辑 | 关键词确定性分类，优先级：售后 > 优惠 > 履约 > 商品 > 澄清 > 兜底 |
| 特殊规则 | 对比意图/可行性疑问/高端意向直接放行 product，不被缺槽位拦截 |
| 完成条件 | 返回明确路由结果 |
| 异常处理 | 无法分类时走 fallback |
| 枚举值 | route ∈ {product, promotion, fulfillment, aftersales, clarify, fallback} |

#### 节点 3：商品参数 Agent

| 字段 | 说明 |
|---|---|
| 节点名称 | Product Agent / 商品参数推荐 |
| 输入 | 用户消息 + 槽位（预算/尺寸/用途/刷新率） |
| 输出 | 推荐商品列表 + 引用文档 + 推荐话术 |
| 处理逻辑 | RAG 检索知识文档 → 结构化筛选（硬约束）→ 加权打分排序 → Top3 |
| 打分权重 | 用途匹配×3 + 尺寸匹配×2 + 价格贴近预算 + 亮度（明亮客厅） |
| 完成条件 | 返回 ≥1 款候选或无候选提示 |
| 异常处理 | 无候选时返回放宽建议，不编造商品 |
| 特殊能力 | 型号对比模式（"A和B哪个好"直接返回参数对比表） |

#### 节点 4：比价优惠 Agent

| 字段 | 说明 |
|---|---|
| 节点名称 | Promotion Agent / 比价优惠 |
| 输入 | 用户消息 + 商品价格（可选） |
| 输出 | 可用促销列表 + 叠加计算 + 到手价估算 |
| 处理逻辑 | RAG 检索促销规则 → 贪心算法按优惠力度叠加（遵守叠加规则） |
| 叠加规则 | 国补可与以旧换新/优惠券叠加；满减可与以旧换新/优惠券叠加；国补不与满减叠加 |
| 完成条件 | 返回促销列表和到手价 |
| 异常处理 | 无匹配促销时提示关注活动 |
| 枚举值 | promotion_type ∈ {国补, 以旧换新, 满减, 优惠券, 限时折扣, 价保} |

#### 节点 5：履约服务 Agent

| 字段 | 说明 |
|---|---|
| 节点名称 | Fulfillment Agent / 履约服务 |
| 输入 | 用户消息 + 地区（可选） |
| 输出 | 配送/安装/入户政策说明 |
| 处理逻辑 | RAG 检索履约政策库 → 按问题类型组装回答 |
| 完成条件 | 返回相关政策说明 |
| 异常处理 | 检索不到时返回通用政策兜底 |
| 提示 | 配送时效问题提示提供地址获取精确信息 |

#### 节点 6：售后客服 Agent

| 字段 | 说明 |
|---|---|
| 节点名称 | Aftersales Agent / 售后客服 |
| 输入 | 用户消息（含售后关键词） |
| 输出 | 问题分类 + 转人工话术 + 需补充信息 |
| 处理逻辑 | 关键词分类问题类型 → 检查订单号 → 生成转人工话术 |
| 核心原则 | **不自行处理售后问题，强制转人工**（Human in the Loop） |
| 完成条件 | 返回转人工话术 |
| 异常处理 | 始终转人工，无异常路径 |
| 枚举值 | issue_type ∈ {退货, 换货, 退款, 维修, 投诉, 发票, 质量问题} |

#### 节点 7：需求澄清 Agent

| 字段 | 说明 |
|---|---|
| 节点名称 | Clarify Agent / 需求澄清 |
| 输入 | 当前槽位 + 澄清次数 |
| 输出 | 澄清问题（带选项）+ 缺失字段 |
| 处理逻辑 | 按优先级（预算 > 尺寸/距离 > 用途 > 刷新率）找出第一个缺失项，生成带选项的问题 |
| 终止条件 | 硬约束齐全 或 澄清达3次上限 |
| 异常处理 | 3次未补全时按默认场景（4000/55寸/追剧）给出推荐 |
| 设计原则 | 给选项而非开放式问题，减少用户输入成本 |

#### 节点 8：Replanner（约束检查）

| 字段 | 说明 |
|---|---|
| 节点名称 | Replanner / 硬约束二次检查 |
| 输入 | 候选商品列表 + 用户槽位 |
| 输出 | 过滤后的商品列表 + 检查结果 |
| 检查项 | 预算合规、刷新率达标（游戏场景≥120Hz）、有库存、无越权请求 |
| 处理逻辑 | 移除违规候选 → 全部违规时返回放宽建议 |
| 核心原则 | **无候选时拒绝编造推荐** |
| 完成条件 | 返回合规候选或降级建议 |

#### 节点 9：Compliance（合规审核）

| 字段 | 说明 |
|---|---|
| 节点名称 | Compliance Reflection / 合规审核 |
| 输入 | 待审核回答 + 候选商品 + 路由 |
| 输出 | 审核结果 + 修正后文本 |
| 5条红线 | ①不编造参数 ②不做绝对承诺 ③不贬低竞品 ④价格有依据 ⑤高风险转人工 |
| 修正循环 | 不通过时自动修正，最大2次；2次仍不通过则降级安全模板 |
| 温度 | temperature=0（确定性检查） |
| 完成条件 | 通过审核或降级 |

---

## 四、评测方案

### 4.1 评测集设计

25 条评测用例，按三类分层：

| 类别 | 数量 | 达标线 | 覆盖场景 |
|---|---|---|---|
| 正常 Case | 15 | ≥90% | 完整约束推荐、优惠查询、履约查询、多轮澄清、中文数字、游戏电视、老人用、明亮客厅、型号对比、以旧换新、配送时效、安装费用、大屏推荐、租房低价 |
| 边界 Case | 7 | ≥50% | 否定句、口语距离、3k缩写、高预算、极低预算、模糊需求、缺尺寸游戏电视 |
| 异常 Case | 3 | ≤1%异常率 | 退货转人工、投诉转人工、非相关问题兜底 |

### 4.2 评测维度

每条用例检查：
1. **路由正确性**：是否路由到正确 Agent
2. **槽位提取**：预算/尺寸/距离/用途是否正确提取
3. **行为合规**：预算是否合规、引用是否完整、高风险是否转人工、兜底是否引导 |

### 4.3 评测方式

- **自动化评测**：`python eval.py` 批量运行，输出分层通过率
- **LLM-as-Judge**（接入 LLM 后）：用大模型对回答质量打分，减少人工评测成本
- **人工抽检**：关键场景和高风险回复必须人工复核
- **持续回归**：每次 Prompt/策略变更后全量回归

### 4.4 V1.0 → V1.1 评测结果

两轮评测使用同一套 25 条模拟测试用例，按正常、边界、异常三层组织：

| 版本 | 通过数 | 通过率 | 迭代结果 |
|---|---:|---:|---|
| V1.0 | 18/25 | 72% | 发现知识库外型号幻觉、槽位解析和售后转人工等问题 |
| V1.1 | 23/25 | 92% | 知识库外型号幻觉 4 次→0 次，售后未转人工 1 次→0 次 |

V1.1 仍有 2 条用例未满分，集中在国补和以旧换新的具体估算，列入后续迭代。

---

## 五、上线迭代机制

### 5.1 三层监控体系

#### 第一层：业务监控（最终结果）

| 指标 | 定义 | 目标 | 告警阈值 |
|---|---|---|---|
| 自助解决率 | 无需转人工的对话占比 | ≥80% | <70% |
| 用户满意度 | 对话结束后评分 | ≥4.0/5 | <3.5 |
| 推荐点击率 | 推荐商品被点击的比例 | ≥30% | <20% |
| 转人工率 | 触发转人工的对话占比 | ≤20% | >30% |

#### 第二层：执行监控（系统行为）

| 指标 | 定义 | 目标 | 告警阈值 |
|---|---|---|---|
| 意图分类准确率 | Master Router 分类正确比例 | ≥95% | <90% |
| 节点通过率 | 各 Worker Agent 成功执行比例 | ≥95% | <90% |
| 硬约束满足率 | Replanner 检查通过比例 | 100% | <100%（必须告警） |
| 合规通过率 | Compliance 一次通过比例 | ≥90% | <80% |
| 澄清轮次 | 平均澄清次数 | ≤2轮 | >3轮 |
| 回复时长 | 从请求到返回的时间 | ≤3秒 | >5秒 |

#### 第三层：成本监控（资源消耗）

| 指标 | 定义 | 目标 | 告警阈值 |
|---|---|---|---|
| 单次 Token 消耗 | 平均每次对话 Token 数 | ≤3000 | >5000 |
| 弱模型占比 | 简单任务使用弱模型的比例 | ≥80% | <60% |
| 缓存命中率 | 结果缓存命中比例 | ≥30% | <10% |
| LLM 调用次数 | 平均每次对话 LLM 调用数 | ≤2次 | >4次 |
| 日成本 | 每日 API 调用总成本 | 预算内 | 超预算 |

### 5.2 Bad Case 回收机制

1. **自动采集**：Replanner 检查失败、合规修正失败、用户点踩的对话自动标记
2. **人工复核**：每日抽样 50 条 Bad Case，归因分类（路由错误/槽位错误/检索缺失/幻觉/合规问题）
3. **迭代闭环**：
   - 路由错误 → 补充 Master Router 关键词规则
   - 槽位错误 → 扩展 NeedParser 正则
   - 检索缺失 → 补充知识库文档
   - 幻觉问题 → 强化 Replanner 约束和合规审核
   - 合规问题 → 补充红线规则
4. **版本管理**：每次迭代更新评测集，全量回归后发布

### 5.3 迭代节奏

| 阶段 | 周期 | 内容 |
|---|---|---|
| V1.0 | 已完成 | 基础架构 + 25条评测，18/25 通过 |
| V1.1 | 1周 | 接入 LLM 润色 + LLM-as-Judge 评测 + 结果缓存 |
| V1.2 | 2周 | 多轮对话优化 + 长期记忆启用 + 用户画像 |
| V2.0 | 1月 | 对接真实商品 API + A/B 测试框架 + 灰度放量 |

---

## 六、兜底策略

| 失败场景 | 兜底方案 |
|---|---|
| LLM 调用失败（3次重试后） | 降级为确定性模板回复，架构和约束检查不变 |
| RAG 检索无结果 | 返回通用选购建议，提示咨询客服 |
| 商品筛选无候选 | 给出放宽约束的建议（提高预算/放宽刷新率/考虑主流尺寸） |
| 合规审核2次修正失败 | 降级为安全模板："建议咨询在线客服获取最新信息" |
| 售后高风险 | 强制转人工，不自行处理 |
| 澄清3次仍信息不足 | 按默认场景（4000/55寸/追剧）给出推荐，提示可补充需求 |
| 系统异常 | 返回友好提示，记录错误日志 |

---

## 七、成本优化设计

1. **模型分级**：意图分类用确定性规则（0成本），简单问答用弱模型，复杂推荐用强模型
2. **Prompt 缓存**：系统提示词和角色设定复用，减少输入 Token
3. **结果缓存**：高频查询（如优惠活动、配送政策）结果缓存，有效期24小时
4. **并行节点**：商品/优惠/履约 Agent 可并行调用，减少总耗时
5. **上下文压缩**：每个 Agent 只加载所需上下文，不全量塞入历史对话
6. **确定性优先**：能用规则解决的不用 LLM（分类/筛选/约束检查全部规则实现）
