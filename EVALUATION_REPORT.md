# 评测报告 — 电视选购 Copilot Multi-Agent 导购系统

**版本**：V1.0 → V1.1  
**评测日期**：2026-08-22  
**评测模式**：演示模式（无 LLM，确定性模板）  
**评测脚本**：`eval.py`  
**评测集**：`data/eval_cases.json`（25 条）

---

## 一、评测结果总览

| 版本 | 用例数 | 通过数 | 通过率 | 主要结果 |
|---|---:|---:|---:|---|
| V1.0 | 25 | 18 | **72%** | 暴露知识库外型号幻觉、槽位解析和售后转人工问题 |
| V1.1 | 25 | 23 | **92%** | 知识库外型号幻觉 4 次→0 次，售后未转人工 1 次→0 次 |

---

## 二、评测集设计

### 2.1 三类 Case 定义（来自课程笔记）

| 类别 | 定义 | 达标标准 | 覆盖场景 |
|---|---|---|---|
| 正常 Case | 常规、高频的正常需求 | 准确率 ≥90% | 完整约束推荐、优惠查询、履约查询、多轮澄清、中文数字、游戏电视、老人用、明亮客厅、型号对比、以旧换新、配送时效、安装费用、大屏推荐、租房低价 |
| 边界 Case | 边缘、少见但可能出现的场景 | 准确率 ≥50% | 否定句、口语距离"2米5"、缩写"3k"、高预算、极低预算、模糊需求、缺尺寸游戏电视 |
| 异常 Case | 恶意输入、违规内容、系统异常 | 异常率 ≤1% | 退货转人工、投诉转人工、非相关问题兜底 |

### 2.2 检查维度

每条用例检查以下维度：
- **路由正确性**：Master Router 是否将消息路由到正确的 Worker Agent
- **槽位提取**：NeedParser 是否正确提取预算/尺寸/距离/用途/刷新率
- **行为检查**：回答是否满足该场景的关键行为（如预算合规、引用完整、转人工、兜底引导等）

---

## 三、关键能力验证

### 3.1 意图分类（Master Router）

- 6 类路由（product / promotion / fulfillment / aftersales / clarify / fallback）全部覆盖
- 确定性规则分类，不依赖 LLM，无分类幻觉
- 对比意图、可行性疑问、高端意向等特殊场景正确放行，不被缺槽位拦截

### 3.2 需求解析（NeedParser）

验证通过的解析能力：
- 预算：阿拉伯数字、中文数字"三千"、缩写"3k/4k"、"X万"、"数字+以内/左右"
- 排除分辨率误匹配：4K/8K 不被误解析为预算
- 尺寸：数字+寸、模糊描述"小一点/大一点"
- 距离：小数"2.5米"、口语"2米5"
- 刷新率：数字+Hz
- 用途：10 类用途识别，含否定句处理（"不玩游戏只追剧"→只保留追剧）
- 明亮客厅："客厅很亮""白天也看"等多种表述

### 3.3 硬约束检查（Replanner）

- 预算超支商品自动移除
- 刷新率不达标自动过滤
- 无库存商品排除
- 无候选时拒绝编造，给出放宽建议

### 3.4 合规审核（Compliance）

- 5 条红线自检：不编造参数、不做绝对承诺、不贬低竞品、价格有依据、高风险转人工
- 修正循环：不通过时自动修正，最大 2 次
- 2 次仍不通过则降级为安全模板

### 3.5 售后转人工（Human in the Loop）

- 退货、换货、退款、投诉、维权等高风险关键词触发即时转人工
- 转人工前收集问题类型和订单号状态
- 不自行处理售后问题

### 3.6 四层终止条件

| 终止类型 | 实现方式 |
|---|---|
| 完成条件 | Worker 成功 + 合规通过 |
| 失败条件 | Worker 无候选且无降级 / 合规 2 次修正失败 |
| 中断条件 | 售后高风险 / 用户要求人工 |
| 防重复条件 | 澄清最多 3 次 / 合规修正最多 2 次 / 主循环最多 8 轮 |

---

## 四、V1.0 → V1.1 迭代记录

评测过程中发现并修复的问题：

| # | 问题 | 修复方式 |
|---|---|---|
| 1 | f-string 嵌套引号语法错误 | 拆分为中间变量 |
| 2 | "今天天气"因历史槽位误路由到 product | 增加非相关内容检测 |
| 3 | "3k预算"中 `k\b` 在中文前不匹配（Python3 \w 匹配中文） | 改用前瞻 `(?![a-zA-Z0-9])` |
| 4 | "E8K"被误解析为预算 8000 | 排除 4K/8K 分辨率术语 |
| 5 | "预算1万"不支持 | 增加"万"单位解析 |
| 6 | "客厅很亮"未识别为明亮客厅 | 扩展关键词（很亮/白天看/光线强） |
| 7 | "下单后几天能送到"未匹配履约关键词 | 扩展关键词（几天能/送到/到货） |
| 8 | "租房过渡用"被误判为非相关 | 扩展 tv_related 和商品关键词 |
| 9 | 对比意图被缺槽位拦截走澄清 | 对比/可行性/高端意向直接放行 product |
| 10 | "小一点的"未推断尺寸 | 增加模糊尺寸描述解析 |
| 11 | 履约 RAG 检索不到时无兜底 | 增加通用政策兜底 |
| 12 | 评测用例 check_points 键名不匹配 | 修正为 gaming_use_case |

---

## 五、已知限制

1. **演示模式无 LLM**：当前评测在无 API Key 环境下运行，推荐话术使用确定性模板。接入 LLM 后表达质量会提升，但路由/约束/合规逻辑不变。
2. **商品库为演示数据**：10 款电视为虚构数据，实际部署需对接真实商品 API。
3. **对比功能有限**：型号匹配基于关键词，复杂型号可能匹配不准确。
4. **无真实用户记忆**：长期记忆功能已实现，但评测中未启用（需用户显式授权）。
5. **促销叠加计算为简化版**：实际业务中优惠规则更复杂，需对接促销系统。

---

## 六、后续优化方向

1. 接入 LLM 后补充 LLM-as-Judge 自动评分
2. 增加多轮对话评测集（当前以单轮为主）
3. 补充压力测试和并发测试
4. 对接真实商品数据后重新评测
5. 增加 A/B 测试框架，对比不同 Prompt/策略效果
