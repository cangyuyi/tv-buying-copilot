# 电视选购 Copilot — 项目上下文记录

## 项目目标
构建一个完全自研代码、可独立运行、不依赖第三方 Agent 平台的电视选购 Multi-Agent 系统。零第三方依赖，clone 即跑，无 API Key 时降级为确定性模板模式。

## 技术约束
- Python 3.10+，零第三方依赖（仅标准库）
- LLM 走 OpenAI-compatible API，无 Key 时降级确定性演示
- 所有文件保存到 D:\ai\tv-shopping-copilot\

## 架构决策记录

### 为什么用 Multi-Agent 而非单 Agent
电视选购场景涉及商品推荐、优惠计算、履约查询、售后处理四类差异很大的任务，单 Agent 容易混淆职责。拆分为 Master 路由 + 5 Worker + 合规审核后，每个 Agent 职责单一，Prompt 更短，约束更易执行。

### 为什么确定性路由而非 LLM 分类
6 类意图（商品/优惠/履约/售后/澄清/兜底）的关键词边界清晰，用规则分类 0 成本、100% 可复现，避免 LLM 分类的随机性和延迟。特殊意图（对比/可行性/高端意向）用白名单放行。

### 为什么自研 RAG 而非向量数据库
当前知识库规模小（10 款商品 + 20 篇文档/规则），token overlap 检索足够且零依赖。商品筛选走结构化硬约束（预算/尺寸/刷新率/库存），比纯语义检索更精准。知识库扩大后可平滑升级为向量检索。

### 为什么合规审核独立成节点
推荐话术可能出现编造参数、绝对承诺、贬低竞品等问题。独立合规节点做 Reflection 自检 + 修正循环，将安全检查与业务逻辑解耦，5 条红线可独立迭代。

## 创建时间
2026-08-22
